version: '3'

services:
  ci-build:
    image: microsoft/aspnetcore-build:1.0-1.1
    volumes:
      - .:/src
    working_dir: /src
    command: /bin/bash -c "dotnet restore ./FiapAula6.sln && dotnet publish ./FiapAula6.sln -c Release -o ./obj/Docker/publish"
